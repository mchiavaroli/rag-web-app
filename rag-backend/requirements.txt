# requirements.txt — LLM Wiki backend
# Dipendenze per: Document Intelligence + LLM Wiki

# PDF Processing
pypdf>=3.0.0
PyMuPDF>=1.23.0        # Estrazione immagini bitmap dai PDF (fallback senza DI)
Pillow>=10.0.0

# LLM & AI
anthropic>=0.28.0      # Claude via Azure AI Foundry (AnthropicFoundry)
openai>=1.0.0          # GPT + Mistral via Azure AI Foundry

# Azure AI Services
azure-ai-formrecognizer>=3.3.0   # Azure Document Intelligence
azure-core>=1.29.0

# API Server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9    # Upload file in FastAPI

# HTTP
requests>=2.31.0       # Mistral Document AI (OCR API nativa)

# Document Processing
python-docx>=0.8.11
openpyxl>=3.0.10

# Utils
tqdm>=4.65.0
